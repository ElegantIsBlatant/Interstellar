const config = {
  challenge: tru3, // Set to true if you want to enable password protection.
  users: {
    // You can add multiple users by doing username: 'password'.
    Me: "1",
  },
};

export default config;
